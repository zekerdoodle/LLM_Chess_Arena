# LLM Chess Arena Configuration
# API keys are loaded from environment variables (see .env.example)

api_keys: {}

# Default models for the chess arena
primary_model: gpt-4o
fallback_model: gemini-2.0-flash

# Token budget for model calls
max_token_budget: 128000

# Logging settings
logging:
  level: INFO
